<h2>会員専用ページ</h2>

<p>ようこそ、<%= current_user.name %> さん！</p>

<p>こちらは会員専用のページです。</p>

<div class="profile">
  <div class="profile-image">
    <% if @user.avatar.attached? %>
      <%= image_tag @user.avatar, class: "avatar" , width: 150, height: 180%>
    <% else %>
      <%= image_tag "default-avatar.jpg", class: "avatar", width: 150, height: 180 %>
    <% end %>
  </div>

  <div class="profile-info">
    <p><strong>氏名:</strong> <%= @user.name %></p>
    <p><strong>メールアドレス:</strong> <%= @user.email %></p>
    <p><strong>電話番号:</strong> <%= @user.phone_number %></p>
  </div>

  <div class="actions">
    <%= link_to "会員情報編集", edit_users_mypage_path, class: "btn btn-warning" %>
    <%= link_to "予約一覧", users_reservations_path, class: "btn btn-secondary" %>
    <%= link_to "レビュー一覧", users_reviews_path, class: "btn btn-secondary" %>
  </div>
</div>